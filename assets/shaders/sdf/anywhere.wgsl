#define_import_path shaders::anywhere

// params.x is width ratio
// params.y is height ratio
// params.z is rounding ratio for all four corners
// params.w is unused
fn sdf(p: vec2<f32>) -> f32 {
    return -1.0;
}